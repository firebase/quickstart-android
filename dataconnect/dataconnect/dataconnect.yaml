specVersion: "v1alpha"
serviceId: "dataconnect"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "cloud-sql-instance"
connectorDirs: ["./connectors"]
