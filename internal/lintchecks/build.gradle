plugins {
    id 'com.android.library'
}
android {
    compileSdkVersion 32

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 32
    }
}

dependencies {
    lintChecks project(":internal:lint")
}
